pluginManagement {
    repositories {
        google {
            url "https://maven.google.com"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google {
            url "https://maven.google.com"
        }
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Sanskriti"
include ':app'
